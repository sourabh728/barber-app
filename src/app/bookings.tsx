import { View, Text } from "react-native";

export default function Bookings() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text style={{ fontSize: 28 }}>Bookings Screen ✂️</Text>
    </View>
  );
}